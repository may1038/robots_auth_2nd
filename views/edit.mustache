<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Edit Page</title>
  </head>
  <body>

    <h1 class="editHeader">Edit your profile</h1>
    <div class="main">
      <h1>{{errorMessage}}</h1>
      <form class="" action="/users/{{user._id}}" method="post">

        <div class="">
          <input type="text" name="name" value="{{user.name}}" placeholder="Name">
        </div>
        <div class="">
          <input type="url" name="avatar" value="{{user.avatar}}" placeholder="Avatar">
        </div>

        <div class="">
          <input type="email" name="email" value="{{user.email}}" placeholder="Email">
        </div>

        <div class="">
          <input type="text" name="phone" value="{{user.phone}}" placeholder="Phone Number">
        </div>

        <div class="">
          <input type="text" name="university" value="{{user.university}}" placeholder="University">
        </div>

        <div class="">
          <input type="text" name="company" value="{{user.company}}" placeholder="Company">
        </div>

        <div class="">
          <input type="text" name="job" value="{{user.job}}" placeholder="Job">
        </div>

        <div class="">
          <input type="text" name="skills" value="{{user.skills}}" placeholder="Skills">
        </div>

        <div class="">
          <input class="submitBtn" type="submit" name="" value="Edit Robot">
        </div>
    </form>

  </div>
  </body>
</html>
